/**
 * Warm drop shadow that grounds cream cards against the sunset gradient.
 * One recipe everywhere so every card floats at the same height.
 */
export const cardShadow = {
  shadowColor: "#40060F",
  shadowOpacity: 0.2,
  shadowRadius: 13,
  shadowOffset: { width: 0, height: 8 },
  elevation: 4,
} as const;
